# SCA Client

Standalone Next.js frontend. **Guide docs are at the workspace root** (`../` — see parent `README.md`).

```bash
cp .env.example .env.local
npm install
npm run dev    # http://localhost:3000
```

Read before coding: `../AGENT-LANES.md`, `../API-CONTRACT.md`, `../cursorrules`.
